# Dependencias
node_modules/

# Compilados
dist/
.angular/
*.tsbuildinfo

# Ambiente y secretos
.env
.env.*
!.env.example
src/environments/environment.local.ts

# Datos con informacion personal de familias.
# NUNCA se versionan: son datos sensibles bajo Ley 1581 de 2012.
datos/
*.nominal.csv
*.nominal.xlsx
exportes/

# Sistema operativo y editores
.DS_Store
Thumbs.db
*.log
deck.html

# Capturas que deja la prueba de punta a punta. Se regeneran en cada corrida,
# son binarias y chocan en cada fusion sin aportar nada. Las del deck, que si
# se versionan a proposito, viven en docs/capturas.
frontend/preview-*.png

# Los PDF del manual se generan con docs/armar-pdf.mjs desde docs/manual.html.
# No se versionan: son 1,3 MB cada uno que cambian entero con cada retoque, y el
# archivo del que salen si esta en el repositorio.
docs/generado/
